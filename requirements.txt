# Core web app
Django==6.0.6
gunicorn==23.0.0
whitenoise==6.11.0

# Face engine: ArcFace (optimized model, 512-D) via InsightFace/ONNXRuntime
insightface==1.0.1
onnxruntime==1.27.0
opencv-python==5.0.0.93
numpy==2.5.1

# Face engine: dlib baseline (128-D) -- needs a C++ toolchain on some
# platforms if no prebuilt wheel is available for your Python version.
dlib==20.0.1
face-recognition==1.3.0
# face_recognition_models (an unmaintained dependency of face-recognition)
# still imports the deprecated `pkg_resources` API, which modern setuptools
# (>=81) removed entirely. Pin below <81 so it keeps working.
setuptools<81

# BAB IV evaluation scripts (ROC/EER, robustness, speed, one-shot+TTA)
matplotlib==3.11.0
scikit-learn==1.9.0
scikit-image==0.26.0
